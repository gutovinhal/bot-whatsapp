// Subcomando: bloquearcmdglobal
module.exports = async function bloquearcmdglobal({ message }) {
  await message.reply(
    'Função de bloquear/desbloquear comandos globalmente ainda não implementada.'
  )
}
